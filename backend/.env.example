PORT=3333
NODE_ENV=development

SESSION_SECRET=change-me
SESSION_TTL_DAYS=30
CORS_ORIGINS=https://pilot.skolapp.se,http://localhost:19006
PILOT_RETURN_TOKEN=false
PII_ENC_KEY=
PII_HASH_KEY=
PRIVACY_POLICY_VERSION=1
RETENTION_DAYS_MESSAGES=365
PRIVACY_EXPORT_RATE_PER_IP=5
PRIVACY_ERASE_RATE_PER_IP=3

ADMIN_BOOTSTRAP_TOKEN=...
ADMIN_API_KEY=...
INVITE_RATE_LIMIT_PER_IP=10
VERIFY_RATE_LIMIT_PER_IP=20

AUTH_PROVIDER=magic

# BankID prep (avstängt)
BANKID_ENABLED=false
BANKID_BROKER=signicat
BANKID_CLIENT_ID=
BANKID_CLIENT_SECRET=
BANKID_REDIRECT_URI=http://localhost:3333/auth/bankid/callback
BANKID_ENV=sandbox

# Supabase (EU)
SUPABASE_URL=...
SUPABASE_SERVICE_ROLE=...
# Legacy fallback if older env vars are still present
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# SMTP (valfritt – Console log annars)
SMTP_HOST=...
SMTP_PORT=587
SMTP_USER=...
SMTP_PASS=...
SMTP_FROM="SkolApp <no-reply@skolapp.local>"
